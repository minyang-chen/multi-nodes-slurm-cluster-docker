services:
  slurmnode2:
    image: slurm-node:latest
    hostname: slurmnode2
    user: admin
    network_mode: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 2
              capabilities: [gpu]    
    volumes:
      - /mnt/workspace:/home/admin
 #   ports:
 #     - 16818:6818
 #   depends:
 #     - slurmmaster

volumes:
  shared.vol:

networks:
  host:
    name: host
    external: true
